with Ada.Text_IO;

package body LongInt is
   procedure Add(A: in LongInt; B: in LongInt; res: out longInt) is
	begin
	end add;
	
	procedure print(A: in longInt) is
	begin
	end print;
	
	procedure fetch(B: out longInt) is
		c: Character;
	begin
		while (not end_of_line) loop
			get(c);
			
	end fetch;
	
end longInt;

